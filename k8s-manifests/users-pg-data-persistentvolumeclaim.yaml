apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: users-pg-data
  name: users-pg-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
